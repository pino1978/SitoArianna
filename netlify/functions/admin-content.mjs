const allowed={profile:"src/data/profile.json",results:"src/data/results.json",media:"src/data/media.json"};
const response=(statusCode,body)=>({statusCode,headers:{"Content-Type":"application/json","Cache-Control":"no-store"},body:JSON.stringify(body)});
function authorized(context){return Boolean(context?.clientContext?.user)}
async function github(path){const owner=process.env.GITHUB_OWNER||"pino1978",repo=process.env.GITHUB_REPO||"SitoArianna",branch=process.env.GITHUB_BRANCH||"main";const res=await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/${path}?ref=${encodeURIComponent(branch)}`,{headers:{Authorization:`Bearer ${process.env.GITHUB_CONTENT_TOKEN}`,Accept:"application/vnd.github+json","X-GitHub-Api-Version":"2022-11-28"}});if(!res.ok)throw new Error(`Impossibile leggere ${path}`);const data=await res.json();return JSON.parse(Buffer.from(data.content,"base64").toString("utf8"))}
export async function handler(event,context){if(event.httpMethod!=="GET")return response(405,{error:"Metodo non consentito"});if(!authorized(context))return response(401,{error:"Accesso non autorizzato"});if(!process.env.GITHUB_CONTENT_TOKEN)return response(503,{error:"Console non ancora configurata: token contenuti assente"});try{const [profile,results,media]=await Promise.all(Object.values(allowed).map(github));return response(200,{profile,results,media})}catch(error){return response(500,{error:error.message})}}
