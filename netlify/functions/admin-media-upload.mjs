import {S3Client,PutObjectCommand} from "@aws-sdk/client-s3";
import {getSignedUrl} from "@aws-sdk/s3-request-presigner";
const response=(statusCode,body)=>({statusCode,headers:{"Content-Type":"application/json","Cache-Control":"no-store"},body:JSON.stringify(body)});
function authorized(context){return Boolean(context?.clientContext?.user)}
export async function handler(event,context){if(event.httpMethod!=="POST")return response(405,{error:"Metodo non consentito"});if(!authorized(context))return response(401,{error:"Accesso non autorizzato"});const required=["R2_ACCOUNT_ID","R2_ACCESS_KEY_ID","R2_SECRET_ACCESS_KEY","R2_BUCKET","R2_PUBLIC_URL"];if(required.some(key=>!process.env[key]))return response(503,{error:"Archivio foto non ancora configurato"});try{const {fileName,contentType,size}=JSON.parse(event.body||"{}");if(contentType!=="image/webp"||!fileName?.endsWith(".webp"))return response(400,{error:"Formato foto non consentito"});if(!Number.isFinite(size)||size<=0||size>12*1024*1024)return response(400,{error:"Dimensione foto non consentita"});const safe=fileName.replace(/[^a-z0-9._-]/gi,"-");const key=`media/${new Date().getUTCFullYear()}/${safe}`;const client=new S3Client({region:"auto",endpoint:`https://${process.env.R2_ACCOUNT_ID}.r2.cloudflarestorage.com`,credentials:{accessKeyId:process.env.R2_ACCESS_KEY_ID,secretAccessKey:process.env.R2_SECRET_ACCESS_KEY}});const uploadUrl=await getSignedUrl(client,new PutObjectCommand({Bucket:process.env.R2_BUCKET,Key:key,ContentType:"image/webp",ContentLength:size}),{expiresIn:300});return response(200,{uploadUrl,publicUrl:`${process.env.R2_PUBLIC_URL.replace(/\/$/,"")}/${key}`})}catch(error){return response(500,{error:"Impossibile preparare il caricamento della foto"})}}
